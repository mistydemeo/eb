# Makefile.in generated by automake 1.7.8 from Makefile.am.
# @configure_input@

# Copyright 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002, 2003
# Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
top_builddir = ..

am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
INSTALL = @INSTALL@
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
host_triplet = @host@
ACLOCAL = @ACLOCAL@
ALLOCA = @ALLOCA@
AMDEP_FALSE = @AMDEP_FALSE@
AMDEP_TRUE = @AMDEP_TRUE@
AMTAR = @AMTAR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
EBCONF_EBINCS = @EBCONF_EBINCS@
EBCONF_EBLIBS = @EBCONF_EBLIBS@
EBCONF_INTLINCS = @EBCONF_INTLINCS@
EBCONF_INTLLIBS = @EBCONF_INTLLIBS@
EBCONF_ZLIBINCS = @EBCONF_ZLIBINCS@
EBCONF_ZLIBLIBS = @EBCONF_ZLIBLIBS@
EB_VERSION_MAJOR = @EB_VERSION_MAJOR@
EB_VERSION_MINOR = @EB_VERSION_MINOR@
ECHO = @ECHO@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
ENABLE_NLS = @ENABLE_NLS@
ENABLE_PTHREAD = @ENABLE_PTHREAD@
ENABLE_SAMPLES_FALSE = @ENABLE_SAMPLES_FALSE@
ENABLE_SAMPLES_TRUE = @ENABLE_SAMPLES_TRUE@
EXEEXT = @EXEEXT@
EXEEXT_EXE_FALSE = @EXEEXT_EXE_FALSE@
EXEEXT_EXE_TRUE = @EXEEXT_EXE_TRUE@
ICONVINCS = @ICONVINCS@
ICONVLIBS = @ICONVLIBS@
INCLUDED_ZLIB_FALSE = @INCLUDED_ZLIB_FALSE@
INCLUDED_ZLIB_TRUE = @INCLUDED_ZLIB_TRUE@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
INTLINCS = @INTLINCS@
INTLLIBS = @INTLLIBS@
LDFLAGS = @LDFLAGS@
LIBEB_VERSION_INFO = @LIBEB_VERSION_INFO@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIBTOOL = @LIBTOOL@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAILING_ADDRESS = @MAILING_ADDRESS@
MAKEINFO = @MAKEINFO@
MSGFMT = @MSGFMT@
MSGMERGE = @MSGMERGE@
OBJEXT = @OBJEXT@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PERL = @PERL@
PTHREAD_CFLAGS = @PTHREAD_CFLAGS@
PTHREAD_CPPFLAGS = @PTHREAD_CPPFLAGS@
PTHREAD_LDFLAGS = @PTHREAD_LDFLAGS@
RANLIB = @RANLIB@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
XGETTEXT = @XGETTEXT@
ZLIBDEPS = @ZLIBDEPS@
ZLIBINCS = @ZLIBINCS@
ZLIBLIBS = @ZLIBLIBS@
ac_ct_CC = @ac_ct_CC@
ac_ct_RANLIB = @ac_ct_RANLIB@
ac_ct_STRIP = @ac_ct_STRIP@
am__fastdepCC_FALSE = @am__fastdepCC_FALSE@
am__fastdepCC_TRUE = @am__fastdepCC_TRUE@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
datadir = @datadir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
oldincludedir = @oldincludedir@
prefix = @prefix@
program_transform_name = @program_transform_name@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
subdirs = @subdirs@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
info_TEXINFOS = ebfont.texi ebinfo.texi ebrefile.texi ebstopcode.texi \
	ebzip.texi

subdir = doc
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
DIST_SOURCES =
am__TEXINFO_TEX_DIR = $(srcdir)
INFO_DEPS = ebfont.info ebinfo.info ebrefile.info ebstopcode.info \
	ebzip.info
DVIS = ebfont.dvi ebinfo.dvi ebrefile.dvi ebstopcode.dvi ebzip.dvi
PDFS = ebfont.pdf ebinfo.pdf ebrefile.pdf ebstopcode.pdf ebzip.pdf
PSS = ebfont.ps ebinfo.ps ebrefile.ps ebstopcode.ps ebzip.ps
TEXINFOS = ebfont.texi ebinfo.texi ebrefile.texi ebstopcode.texi \
	ebzip.texi
DIST_COMMON = $(srcdir)/Makefile.in Makefile.am mdate-sh stamp-1 \
	stamp-2 stamp-3 stamp-4 stamp-vti texinfo.tex \
	version-ebfont.texi version-ebinfo.texi version-ebrefile.texi \
	version-ebstopcode.texi version-ebzip.texi
all: all-am

.SUFFIXES:
.SUFFIXES: .dvi .info .pdf .ps .texi
$(srcdir)/Makefile.in:  Makefile.am  $(top_srcdir)/configure.ac $(ACLOCAL_M4)
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --gnu  doc/Makefile
Makefile:  $(srcdir)/Makefile.in  $(top_builddir)/config.status
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs

distclean-libtool:
	-rm -f libtool

.texi.info:
	@rm -f $@ $@-[0-9] $@-[0-9][0-9] $(@:.info=).i[0-9] $(@:.info=).i[0-9][0-9]
	$(MAKEINFO) $(AM_MAKEINFOFLAGS) $(MAKEINFOFLAGS) -I $(srcdir) \
	 -o $@ `test -f '$<' || echo '$(srcdir)/'`$<

.texi.dvi:
	TEXINPUTS="$(am__TEXINFO_TEX_DIR)$(PATH_SEPARATOR)$$TEXINPUTS" \
	MAKEINFO='$(MAKEINFO) $(AM_MAKEINFOFLAGS) $(MAKEINFOFLAGS) -I $(srcdir)' \
	$(TEXI2DVI) `test -f '$<' || echo '$(srcdir)/'`$<

.texi.pdf:
	TEXINPUTS="$(am__TEXINFO_TEX_DIR)$(PATH_SEPARATOR)$$TEXINPUTS" \
	MAKEINFO='$(MAKEINFO) $(AM_MAKEINFOFLAGS) $(MAKEINFOFLAGS) -I $(srcdir)' \
	$(TEXI2PDF) `test -f '$<' || echo '$(srcdir)/'`$<
ebfont.info: ebfont.texi version-ebfont.texi
ebfont.dvi: ebfont.texi version-ebfont.texi
ebfont.pdf: ebfont.texi version-ebfont.texi
version-ebfont.texi:  stamp-vti
stamp-vti: ebfont.texi $(top_srcdir)/configure
	@(dir=.; test -f ./ebfont.texi || dir=$(srcdir); \
	set `$(SHELL) $(srcdir)/mdate-sh $$dir/ebfont.texi`; \
	echo "@set UPDATED $$1 $$2 $$3"; \
	echo "@set UPDATED-MONTH $$2 $$3"; \
	echo "@set EDITION $(VERSION)"; \
	echo "@set VERSION $(VERSION)") > vti.tmp
	@cmp -s vti.tmp version-ebfont.texi \
	  || (echo "Updating version-ebfont.texi"; \
	      cp vti.tmp version-ebfont.texi)
	-@rm -f vti.tmp
	@cp version-ebfont.texi $@

mostlyclean-vti:
	-rm -f vti.tmp

maintainer-clean-vti:
	-rm -f stamp-vti version-ebfont.texi
ebinfo.info: ebinfo.texi version-ebinfo.texi
ebinfo.dvi: ebinfo.texi version-ebinfo.texi
ebinfo.pdf: ebinfo.texi version-ebinfo.texi
version-ebinfo.texi:  stamp-1
stamp-1: ebinfo.texi $(top_srcdir)/configure
	@(dir=.; test -f ./ebinfo.texi || dir=$(srcdir); \
	set `$(SHELL) $(srcdir)/mdate-sh $$dir/ebinfo.texi`; \
	echo "@set UPDATED $$1 $$2 $$3"; \
	echo "@set UPDATED-MONTH $$2 $$3"; \
	echo "@set EDITION $(VERSION)"; \
	echo "@set VERSION $(VERSION)") > 1.tmp
	@cmp -s 1.tmp version-ebinfo.texi \
	  || (echo "Updating version-ebinfo.texi"; \
	      cp 1.tmp version-ebinfo.texi)
	-@rm -f 1.tmp
	@cp version-ebinfo.texi $@

mostlyclean-1:
	-rm -f 1.tmp

maintainer-clean-1:
	-rm -f stamp-1 version-ebinfo.texi
ebrefile.info: ebrefile.texi version-ebrefile.texi
ebrefile.dvi: ebrefile.texi version-ebrefile.texi
ebrefile.pdf: ebrefile.texi version-ebrefile.texi
version-ebrefile.texi:  stamp-2
stamp-2: ebrefile.texi $(top_srcdir)/configure
	@(dir=.; test -f ./ebrefile.texi || dir=$(srcdir); \
	set `$(SHELL) $(srcdir)/mdate-sh $$dir/ebrefile.texi`; \
	echo "@set UPDATED $$1 $$2 $$3"; \
	echo "@set UPDATED-MONTH $$2 $$3"; \
	echo "@set EDITION $(VERSION)"; \
	echo "@set VERSION $(VERSION)") > 2.tmp
	@cmp -s 2.tmp version-ebrefile.texi \
	  || (echo "Updating version-ebrefile.texi"; \
	      cp 2.tmp version-ebrefile.texi)
	-@rm -f 2.tmp
	@cp version-ebrefile.texi $@

mostlyclean-2:
	-rm -f 2.tmp

maintainer-clean-2:
	-rm -f stamp-2 version-ebrefile.texi
ebstopcode.info: ebstopcode.texi version-ebstopcode.texi
ebstopcode.dvi: ebstopcode.texi version-ebstopcode.texi
ebstopcode.pdf: ebstopcode.texi version-ebstopcode.texi
version-ebstopcode.texi:  stamp-3
stamp-3: ebstopcode.texi $(top_srcdir)/configure
	@(dir=.; test -f ./ebstopcode.texi || dir=$(srcdir); \
	set `$(SHELL) $(srcdir)/mdate-sh $$dir/ebstopcode.texi`; \
	echo "@set UPDATED $$1 $$2 $$3"; \
	echo "@set UPDATED-MONTH $$2 $$3"; \
	echo "@set EDITION $(VERSION)"; \
	echo "@set VERSION $(VERSION)") > 3.tmp
	@cmp -s 3.tmp version-ebstopcode.texi \
	  || (echo "Updating version-ebstopcode.texi"; \
	      cp 3.tmp version-ebstopcode.texi)
	-@rm -f 3.tmp
	@cp version-ebstopcode.texi $@

mostlyclean-3:
	-rm -f 3.tmp

maintainer-clean-3:
	-rm -f stamp-3 version-ebstopcode.texi
ebzip.info: ebzip.texi version-ebzip.texi
ebzip.dvi: ebzip.texi version-ebzip.texi
ebzip.pdf: ebzip.texi version-ebzip.texi
version-ebzip.texi:  stamp-4
stamp-4: ebzip.texi $(top_srcdir)/configure
	@(dir=.; test -f ./ebzip.texi || dir=$(srcdir); \
	set `$(SHELL) $(srcdir)/mdate-sh $$dir/ebzip.texi`; \
	echo "@set UPDATED $$1 $$2 $$3"; \
	echo "@set UPDATED-MONTH $$2 $$3"; \
	echo "@set EDITION $(VERSION)"; \
	echo "@set VERSION $(VERSION)") > 4.tmp
	@cmp -s 4.tmp version-ebzip.texi \
	  || (echo "Updating version-ebzip.texi"; \
	      cp 4.tmp version-ebzip.texi)
	-@rm -f 4.tmp
	@cp version-ebzip.texi $@

mostlyclean-4:
	-rm -f 4.tmp

maintainer-clean-4:
	-rm -f stamp-4 version-ebzip.texi
TEXI2DVI = texi2dvi

TEXI2PDF = $(TEXI2DVI) --pdf --batch
DVIPS = dvips
.dvi.ps:
	$(DVIPS) -o $@ $<

uninstall-info-am:
	$(PRE_UNINSTALL)
	@if (install-info --version && \
	     install-info --version 2>&1 | sed 1q | grep -i -v debian) >/dev/null 2>&1; then \
	  list='$(INFO_DEPS)'; \
	  for file in $$list; do \
	    relfile=`echo "$$file" | sed 's|^.*/||'`; \
	    echo " install-info --info-dir=$(DESTDIR)$(infodir) --remove $(DESTDIR)$(infodir)/$$relfile"; \
	    install-info --info-dir=$(DESTDIR)$(infodir) --remove $(DESTDIR)$(infodir)/$$relfile; \
	  done; \
	else :; fi
	@$(NORMAL_UNINSTALL)
	@list='$(INFO_DEPS)'; \
	for file in $$list; do \
	  relfile=`echo "$$file" | sed 's|^.*/||'`; \
	  relfile_i=`echo "$$relfile" | sed 's|\.info$$||;s|$$|.i|'`; \
	  (if cd $(DESTDIR)$(infodir); then \
	     echo " rm -f $$relfile $$relfile-[0-9] $$relfile-[0-9][0-9] $$relfile_i[0-9] $$relfile_i[0-9][0-9])"; \
	     rm -f $$relfile $$relfile-[0-9] $$relfile-[0-9][0-9] $$relfile_i[0-9] $$relfile_i[0-9][0-9]; \
	   else :; fi); \
	done

dist-info: $(INFO_DEPS)
	list='$(INFO_DEPS)'; \
	for base in $$list; do \
	  if test -f $$base; then d=.; else d=$(srcdir); fi; \
	  for file in $$d/$$base*; do \
	    relfile=`expr "$$file" : "$$d/\(.*\)"`; \
	    test -f $(distdir)/$$relfile || \
	      cp -p $$file $(distdir)/$$relfile; \
	  done; \
	done

mostlyclean-aminfo:
	-rm -f ebfont.aux ebfont.cp ebfont.cps ebfont.fn ebfont.fns ebfont.ky \
	  ebfont.kys ebfont.log ebfont.pg ebfont.pgs ebfont.tmp \
	  ebfont.toc ebfont.tp ebfont.tps ebfont.vr ebfont.vrs \
	  ebfont.dvi ebfont.pdf ebfont.ps ebinfo.aux ebinfo.cp \
	  ebinfo.cps ebinfo.fn ebinfo.fns ebinfo.ky ebinfo.kys \
	  ebinfo.log ebinfo.pg ebinfo.pgs ebinfo.tmp ebinfo.toc \
	  ebinfo.tp ebinfo.tps ebinfo.vr ebinfo.vrs ebinfo.dvi \
	  ebinfo.pdf ebinfo.ps ebrefile.aux ebrefile.cp ebrefile.cps \
	  ebrefile.fn ebrefile.fns ebrefile.ky ebrefile.kys \
	  ebrefile.log ebrefile.pg ebrefile.pgs ebrefile.tmp \
	  ebrefile.toc ebrefile.tp ebrefile.tps ebrefile.vr \
	  ebrefile.vrs ebrefile.dvi ebrefile.pdf ebrefile.ps \
	  ebstopcode.aux ebstopcode.cp ebstopcode.cps ebstopcode.fn \
	  ebstopcode.fns ebstopcode.ky ebstopcode.kys ebstopcode.log \
	  ebstopcode.pg ebstopcode.pgs ebstopcode.tmp ebstopcode.toc \
	  ebstopcode.tp ebstopcode.tps ebstopcode.vr ebstopcode.vrs \
	  ebstopcode.dvi ebstopcode.pdf ebstopcode.ps ebzip.aux \
	  ebzip.cp ebzip.cps ebzip.fn ebzip.fns ebzip.ky ebzip.kys \
	  ebzip.log ebzip.pg ebzip.pgs ebzip.tmp ebzip.toc ebzip.tp \
	  ebzip.tps ebzip.vr ebzip.vrs ebzip.dvi ebzip.pdf ebzip.ps

maintainer-clean-aminfo:
	@list='$(INFO_DEPS)'; for i in $$list; do \
	  i_i=`echo "$$i" | sed 's|\.info$$||;s|$$|.i|'`; \
	  echo " rm -f $$i $$i-[0-9] $$i-[0-9][0-9] $$i_i[0-9] $$i_i[0-9][0-9]"; \
	  rm -f $$i $$i-[0-9] $$i-[0-9][0-9] $$i_i[0-9] $$i_i[0-9][0-9]; \
	done
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:

DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)

top_distdir = ..
distdir = $(top_distdir)/$(PACKAGE)-$(VERSION)

distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's|.|.|g'`; \
	list='$(DISTFILES)'; for file in $$list; do \
	  case $$file in \
	    $(srcdir)/*) file=`echo "$$file" | sed "s|^$$srcdirstrip/||"`;; \
	    $(top_srcdir)/*) file=`echo "$$file" | sed "s|^$$topsrcdirstrip/|$(top_builddir)/|"`;; \
	  esac; \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
	    dir="/$$dir"; \
	    $(mkinstalldirs) "$(distdir)$$dir"; \
	  else \
	    dir=''; \
	  fi; \
	  if test -d $$d/$$file; then \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
	$(MAKE) $(AM_MAKEFLAGS) \
	  top_distdir="$(top_distdir)" distdir="$(distdir)" \
	  dist-info
check-am: all-am
check: check-am
all-am: Makefile $(INFO_DEPS)

installdirs:
	$(mkinstalldirs) $(DESTDIR)$(infodir)
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic distclean-libtool

dvi: dvi-am

dvi-am: $(DVIS)

info: info-am

info-am: $(INFO_DEPS)

install-data-am: install-info-am

install-exec-am:

install-info: install-info-am

install-info-am: $(INFO_DEPS)
	@$(NORMAL_INSTALL)
	$(mkinstalldirs) $(DESTDIR)$(infodir)
	@list='$(INFO_DEPS)'; \
	for file in $$list; do \
	  if test -f $$file; then d=.; else d=$(srcdir); fi; \
	  file_i=`echo "$$file" | sed 's|\.info$$||;s|$$|.i|'`; \
	  for ifile in $$d/$$file $$d/$$file-[0-9] $$d/$$file-[0-9][0-9] \
                       $$d/$$file_i[0-9] $$d/$$file_i[0-9][0-9] ; do \
	    if test -f $$ifile; then \
	      relfile=`echo "$$ifile" | sed 's|^.*/||'`; \
	      echo " $(INSTALL_DATA) $$ifile $(DESTDIR)$(infodir)/$$relfile"; \
	      $(INSTALL_DATA) $$ifile $(DESTDIR)$(infodir)/$$relfile; \
	    else : ; fi; \
	  done; \
	done
	@$(POST_INSTALL)
	@if (install-info --version && \
	     install-info --version 2>&1 | sed 1q | grep -i -v debian) >/dev/null 2>&1; then \
	  list='$(INFO_DEPS)'; \
	  for file in $$list; do \
	    relfile=`echo "$$file" | sed 's|^.*/||'`; \
	    echo " install-info --info-dir=$(DESTDIR)$(infodir) $(DESTDIR)$(infodir)/$$relfile";\
	    install-info --info-dir=$(DESTDIR)$(infodir) $(DESTDIR)$(infodir)/$$relfile || :;\
	  done; \
	else : ; fi
install-man:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-1 maintainer-clean-2 \
	maintainer-clean-3 maintainer-clean-4 maintainer-clean-aminfo \
	maintainer-clean-generic maintainer-clean-vti

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-1 mostlyclean-2 mostlyclean-3 mostlyclean-4 \
	mostlyclean-aminfo mostlyclean-generic mostlyclean-libtool \
	mostlyclean-vti

pdf: pdf-am

pdf-am: $(PDFS)

ps: ps-am

ps-am: $(PSS)

uninstall-am: uninstall-info-am

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	dist-info distclean distclean-generic distclean-libtool distdir \
	dvi dvi-am info info-am install install-am install-data \
	install-data-am install-exec install-exec-am install-info \
	install-info-am install-man install-strip installcheck \
	installcheck-am installdirs maintainer-clean maintainer-clean-1 \
	maintainer-clean-2 maintainer-clean-3 maintainer-clean-4 \
	maintainer-clean-aminfo maintainer-clean-generic \
	maintainer-clean-vti mostlyclean mostlyclean-1 mostlyclean-2 \
	mostlyclean-3 mostlyclean-4 mostlyclean-aminfo \
	mostlyclean-generic mostlyclean-libtool mostlyclean-vti pdf \
	pdf-am ps ps-am uninstall uninstall-am uninstall-info-am

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
